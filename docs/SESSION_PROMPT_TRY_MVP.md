# Try the Roundtable Observer MVP (paste into a fresh Claude Code session)

In <repo> (branch feat/observer-mvp, everything already built and tested): run `npm run dev`, open http://localhost:5173 in my default browser, confirm the office renders this live session, then spawn 2-3 short demo subagents (one at a time, cheap models, ~20s of thinking + a couple of file reads each) so I can watch actors walk in, think, and deliver results live — keep both servers running until I say stop, touch nothing else in the repo, and never write into ~/.claude.
